// problem 1 create a sum() function-practice
function sum(sumNumber1, sumNumber2) {
  var sumAnswer = sumNumber1 + sumNumber2;
  console.log('The sum of ' + sumNumber1 + ' and ' + sumNumber2 + ' is ' + sumAnswer + '.');
  return;
}
